# src/utils.py

# Define a constant for the output filename suffix to ensure consistency.
VAD_OUTPUT_FILENAME_SUFFIX = "_vad_timestamps.csv"

# You can add more utility functions here if needed, for example,
# a function to generate a summary report of all processed files.
